name: hello-world
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:      
      - name: hello
        run: ls -la

      - name: linter
        run: make lint

      - name: pytest
        run: make pytest
